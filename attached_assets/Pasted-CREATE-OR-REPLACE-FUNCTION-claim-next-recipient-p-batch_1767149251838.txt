CREATE OR REPLACE FUNCTION claim_next_recipient(p_batch_id INTEGER)
RETURNS TABLE (
    id INTEGER,
    batch_id INTEGER,
    phone_number VARCHAR(50),
    custom_fields JSONB,
    status VARCHAR(50),
    conversation_id VARCHAR(255),
    duration INTEGER,
    transcript TEXT,
    summary TEXT,
    error_message TEXT,
    created_at TIMESTAMP WITH TIME ZONE,
    updated_at TIMESTAMP WITH TIME ZONE,
    completed_at TIMESTAMP WITH TIME ZONE
) AS $$
DECLARE
    claimed_recipient RECORD;
BEGIN
    -- Lock and claim the next pending recipient atomically
    -- FOR UPDATE SKIP LOCKED ensures concurrent workers get different rows
    SELECT bcr.* INTO claimed_recipient
    FROM batch_call_recipients bcr
    WHERE bcr.batch_id = p_batch_id AND bcr.status = 'pending'
    ORDER BY bcr.created_at ASC
    FOR UPDATE SKIP LOCKED
    LIMIT 1;
    
    -- If no recipient found, return empty result
    IF NOT FOUND THEN
        RETURN;
    END IF;
    
    -- Update status to in_progress
    UPDATE batch_call_recipients
    SET status = 'in_progress', updated_at = NOW()
    WHERE batch_call_recipients.id = claimed_recipient.id
    RETURNING * INTO claimed_recipient;
    
    -- Return the claimed recipient
    RETURN QUERY SELECT 
        claimed_recipient.id,
        claimed_recipient.batch_id,
        claimed_recipient.phone_number,
        claimed_recipient.custom_fields,
        claimed_recipient.status,
        claimed_recipient.conversation_id,
        claimed_recipient.duration,
        claimed_recipient.transcript,
        claimed_recipient.summary,
        claimed_recipient.error_message,
        claimed_recipient.created_at,
        claimed_recipient.updated_at,
        claimed_recipient.completed_at;
END;
$$ LANGUAGE plpgsql;
        
✅ Client usage table already exists
📝 Create sms_conversations table in Supabase:
CREATE TABLE IF NOT EXISTS sms_conversations (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255) NOT NULL REFERENCES users(id),
    phone_number VARCHAR(50) NOT NULL,
    message TEXT NOT NULL,
    direction VARCHAR(20) NOT NULL CHECK (direction IN ('inbound', 'outbound')),
    status VARCHAR(20) NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'sent', 'delivered', 'failed')),
    twilio_message_sid VARCHAR(255),
    error_message TEXT,
    metadata JSONB,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
-- Create index for faster conversation lookups
CREATE INDEX IF NOT EXISTS idx_sms_conversations_user ON sms_conversations(user_id, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_sms_conversations_phone ON sms_conversations(phone_number, created_at DESC);
            
📝 Add is_admin column to users table in Supabase (run this SQL in Supabase dashboard):
ALTER TABLE users ADD COLUMN IF NOT EXISTS is_admin BOOLEAN DEFAULT false;
-- Make audamaur@gmail.com an admin
UPDATE users SET is_admin = true WHERE email = 'audamaur@gmail.com';
        
📝 Add webhook_token column to business_info table in Supabase (run this SQL in Supabase dashboard):
-- Add webhook token for ElevenLabs initiation webhook authentication
ALTER TABLE business_info ADD COLUMN IF NOT EXISTS webhook_token TEXT;
-- Optional: Generate a random token for existing users (uncomment if needed)
-- UPDATE business_info SET webhook_token = encode(gen_random_bytes(32), 'hex') WHERE webhook_token IS NULL;
        
✅ Database initialization complete
2:41:10 AM [express] ✅ SkyIQ Dashboard Server running on port 10000
2:41:10 AM [express] 📡 Webhook endpoint: http://localhost:10000/webhook
2:41:10 AM [express] 📊 Dashboard: http://localhost:10000
2:41:10 AM [express] 🏥 Health check: http://localhost:10000/health
2:41:10 AM [express] 🗃️ Database: Supabase Connected
2:41:10 AM [express] 📧 Email notifications: Enabled (inbound only)
2:41:10 AM [express] 🔑 Credentials: Per-user from Supabase (no global env fallback)
==> Your service is live 🎉
==> 
==> ///////////////////////////////////////////////////////////
==> 
==> Available at your primary URL https://www.skyiq.app + 1 more domain
==> 
==> ///////////////////////////////////////////////////////////
🧹 Removing scraped content at index 0
✅ Link and scraped content removed successfully
🔄 Triggering prompt update for user: 2e5a04dd-6cb5-440c-9bba-975b9a63753b
📝 Enhanced prompt with full business context
✅ ElevenLabs agent updated with latest business context
🕷️ Starting website scrape for: excellenceinmotion.org
🔍 Scraping website: https://excellenceinmotion.org
🔄 Trying scraping strategy 1/3 for: https://excellenceinmotion.org
🔄 Triggering prompt update for user: 2e5a04dd-6cb5-440c-9bba-975b9a63753b
✅ Strategy 1 succeeded for: https://excellenceinmotion.org
🔍 Detected JavaScript-rendered page (SPA)
🔄 Switching to Puppeteer for JavaScript content...
🌐 Using Puppeteer for JavaScript-rendered page: https://excellenceinmotion.org
🔍 Using Puppeteer bundled Chrome
⚠️ Fetch failed, trying Puppeteer as fallback: Could not find Chrome (ver. 143.0.7499.169). This can occur if either
 1. you did not perform an installation before running the script (e.g. `npx puppeteer browsers install chrome`) or
 2. your cache path is incorrectly configured (which is: /opt/render/.cache/puppeteer).
For (2), check out our guide on configuring puppeteer at https://pptr.dev/guides/configuration.
🌐 Using Puppeteer for JavaScript-rendered page: https://excellenceinmotion.org
🔍 Using Puppeteer bundled Chrome
❌ Error scraping website: https://excellenceinmotion.org Could not find Chrome (ver. 143.0.7499.169). This can occur if either
 1. you did not perform an installation before running the script (e.g. `npx puppeteer browsers install chrome`) or
 2. your cache path is incorrectly configured (which is: /opt/render/.cache/puppeteer).
For (2), check out our guide on configuring puppeteer at https://pptr.dev/guides/configuration.
📝 Only storing the URL itself for: excellenceinmotion.org
❌ Scraping failed for: excellenceinmotion.org Could not find Chrome (ver. 143.0.7499.169). This can occur if either
 1. you did not perform an installation before running the script (e.g. `npx puppeteer browsers install chrome`) or
 2. your cache path is incorrectly configured (which is: /opt/render/.cache/puppeteer).
For (2), check out our guide on configuring puppeteer at https://pptr.dev/guides/configuration.
📝 Enhanced prompt with full business context
✅ URL stored for: excellenceinmotion.org
✅ ElevenLabs agent updated with latest business context
🔄 Triggering prompt update for user: 2e5a04dd-6cb5-440c-9bba-975b9a63753b
📝 Enhanced prompt with full business context
✅ ElevenLabs agent updated with latest business context
🗑️ Attempting to delete link at index 0 for user 2e5a04dd-6cb5-440c-9bba-975b9a63753b
📋 Current links: [ 'excellenceinmotion.org' ]
📊 Links count: 1, trying to delete index: 0
🧹 Removing scraped content at index 0
✅ Link and scraped content removed successfully
🔄 Triggering prompt update for user: 2e5a04dd-6cb5-440c-9bba-975b9a63753b
📝 Enhanced prompt with full business context
✅ ElevenLabs agent updated with latest business context